<!-- <h1>Songs</h1>

<ul>
  <% @songs.each do |s| %>
    <li><%= link_to s.artist_and_title,  song_path(s) %></li>
  <% end %>
</ul> -->

<h1>Songs</h1>

<ul>
  <% @songs.each do |song| %>
    <li>
      <a href="<%= song_path(song) %>">
        <%= song.artist_name %>
        -
        <%= song.title %>
      </a>
    </li>
  <% end %>
</ul>
