<h1>All Artists</h1>
<% @artists.each do |artist| %>
  <h2>Artist Name:<%=artist.name%></h2>
  <h2>Artist Song Count:<%= pluralize(artist.song_count,"song") %></h2>
  <h2>Artist Songs:</h2>
  <ul>
  <% artist.songs.each do |song| %>
    <li><%= song.title %></li>
  <% end %>
  </ul>
<% end %>
